{% extends 'base.html.twig' %}

{% block title %}Hello ProfileController!
{% endblock %}

{% block body %}
{# <div id="profile_container">
  <div class="side_left">
		{{ include('home/_side.html.twig') }}
  </div>
	<div class="wrapper">
		{{ include("profile/_header.html.twig", [user]) }}
	</div>
  <div><a href="{{ path('app_profile_followers',{user:user.id})}}">followers</a></div><br>
  <div><a href="{{ path('app_profile_follows',{user:user.id})}}">follows</a></div>
</div  #}
<div class="side_left">
		{{ include('home/_side.html.twig') }}
	</div>
	
		
	<main>
		<div class="main_container">
			<div class="main_feed">
				 {{ include("profile/_header.html.twig", [user]) }}
		{{ include("post/_allposts.html.twig", [posts])}}
			</div>
			<div class="side_feed">
				{{ include('home/_side_right.html.twig') }}
			</div>
		</div>
	</main>


{% endblock %}

